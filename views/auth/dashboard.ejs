<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard | Wellness AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="min-h-screen bg-gradient-to-tr from-purple-100 to-blue-100 p-6">
    <div class="max-w-2xl mx-auto bg-white p-6 rounded-2xl shadow-lg space-y-6">
      <div class="flex justify-between items-center">
        <h1 class="text-2xl font-bold text-purple-700">
          Welcome, <%= user.name %>!
        </h1>
        <a href="/logout" class="text-red-500 hover:underline">Logout</a>
      </div>

      <% if (user.isAdmin) { %>
      <p class="text-sm text-green-700 font-medium">👑 You are an admin</p>
      <% } %>

      <h2 class="text-xl font-semibold text-gray-700">Your Wellness Tips</h2>

      <% if (tips.length === 0) { %>
      <p class="text-gray-500">You haven't added any tips yet.</p>
      <% } else { %>
      <ul class="space-y-2">
        <% tips.forEach(tip => { %>
        <li class="bg-gray-100 p-3 rounded-xl">
          <%= tip.content %>
          <div class="text-xs text-gray-500 mt-1">
            Posted on <%= tip.createdAt.toDateString() %>
          </div>
        </li>
        <% }) %>
      </ul>
      <% } %>

      <a
        href="/tips/form"
        class="inline-block bg-purple-600 text-white px-4 py-2 rounded-xl hover:bg-purple-700"
      >
        + Add New AI Tip
      </a>
    </div>
  </body>
</html>
