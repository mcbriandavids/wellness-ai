<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Wellness AI | Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="min-h-screen bg-gradient-to-br from-green-100 to-blue-200 flex items-center justify-center"
  >
    <div class="bg-white p-8 rounded-2xl shadow-lg max-w-md text-center">
      <h1 class="text-3xl font-bold text-green-600 mb-4">
        Welcome to Wellness AI 🌿
      </h1>
      <p class="text-gray-700 mb-6">Your personalized wellness companion.</p>

      <% if (user) { %>
      <p class="text-lg text-gray-800 mb-4">Hello, <%= user.name %>!</p>
      <a
        href="/dashboard"
        class="bg-green-500 text-white px-4 py-2 rounded-xl hover:bg-green-600"
        >Go to Dashboard</a
      >
      <a href="/logout" class="ml-4 text-red-500 hover:underline">Logout</a>
      <% } else { %>
      <a
        href="/login"
        class="text-white bg-green-500 hover:bg-green-600 px-4 py-2 rounded-xl"
        >Login</a
      >
      <a href="/register" class="ml-4 text-green-600 hover:underline"
        >Register</a
      >
      <% } %>
    </div>
  </body>
</html>
